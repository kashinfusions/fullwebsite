console.log("Backend file connected");

// import html elements that will be altered
// (on candle page) price, each select opiton, the add to cart button, the plus and minus quantity selectors, & the number between them
// (cart side menu) number in blue bubble, div for products, the total number on the right, & the checkout button (redirects to stripe)
// (on every page) cart number at the top

// write function to implement the price (write after importing products)
// need to pull price from each specific product into the js and assign variables to them
// strong[class="text-primary display-6"] -- translate into js OR pull a class only
 const priceHeader = document.getElementsByClassName("text-primary display-6"); 